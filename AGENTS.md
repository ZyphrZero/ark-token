# Repository Guidelines

## 项目结构与职责范围

本工具面向明日方舟一图流（[ark.yituliu.cn](https://ark.yituliu.cn/)）浏览器插件，使用森空岛 token 获取账号干员数据，再通过一图流读 token 和写 token 自动更新数据，并支持在浏览器插件中切换多个账号，读 token 和写 token 具有唯一性，每个一图流账号仅有读/写 token。

本项目源码、测试、构建配置位于本仓库（根目录 `E:\yituliu\ark-token`），已按 README 中的目录说明组织。实现所依据的接口、数据结构、调用约定位于以下相邻仓库；处理本项目任务时可以并且应当对这些仓库进行只读检索和核对：

- 移动端apk dump抓包数据：`./skland_dump`
- 后端：`E:\yituliu\BackEndV3`
- 前端：`E:\yituliu\frontend-v2-plus`
- 相关接口文档：`E:\yituliu\ark-token\docs`
- 模拟器抓包环境手册：`E:\yituliu\ark-token\docs\EMULATOR_CAPTURE_SETUP.md`（MuMu 模拟器经 Reqable 代理抓包森空岛 App 的完整配置、证书注入、截图与 UI 操作方法、已知坑；涉及模拟器操作时先读此文档）

本项目根目录 `E:\yituliu\ark-token` 是本任务唯一允许写入和修改的范围。分析阶段可以读取、搜索和比对上述后端、前端仓库，但不得修改、提交、删除其中的文件或安装依赖；如确需改变前后端，必须先取得用户明确授权。不得删除、重命名或弱化本文件中记录的代码路径，也不得在实现本项目功能时擅自删除现有接口、路由、调用链或兼容路径。若新增模块，请同步补充本项目 README 中的目录说明。

## 构建、测试与本地开发

构建、测试和修改均针对本仓库进行，依据 README 与 `package.json` 中声明的命令执行，例如 `npm ci`、`npm run dev`、`npm run build`、`npm test`、`npm run build:operator-table`，不要假设未声明的命令可用。提交前记录实际执行的命令及结果。

## 编码风格与命名

遵循本项目现有的 formatter、linter、TypeScript/后端配置和目录约定，不要为单个改动引入新的风格。若本项目未规定，JavaScript/TypeScript 使用 2 空格缩进，变量和函数采用 `camelCase`，类与组件采用 `PascalCase`，常量采用 `UPPER_SNAKE_CASE`；保持导入有序，并让命名体现账号、token 和数据更新的职责。

## 森空岛数据语义与分层封装

- 消费森空岛接口字段前，语义必须经真实抓包样本核实，不得凭字段名、参考项目（rhodes-headquarters 等）实现或单一形态样本推测；决定性样本存入 `skland_dump/`（文件内注明抓包时间与语义结论），类型注释与 `docs/` 文档回链样本出处。
- 已核实且易误读的语义（详见 `docs/BUILDING_MOOD_API.md`）：会客室 `clue.board` 是已置入系列的紧凑列表（置入 1/3/4/7 号位时仅有 4 个元素），不可按槽位下标判断，必须用 `src/core/status/building.ts` 的 `CLUE_SERIES`/`clueBoardSlots()` 按系列名成员判断；`clue.own` 含已置入线索、上限 10；`sharing=true` 不代表线索板集齐 7 条。
- 森空岛基建快照可能落后游戏内状态数十分钟至数小时（需游戏本体登录/进出基建触发上传），属数据源固有延迟；对比插件显示与游戏截图不一致时，先核对快照 `lastUpdateTime` 再判定插件逻辑是否有误。
- 分层封装：接口类型集中在 `src/core/skland-info.ts`，领域计算/推导封装为 `src/core/status/` 下的纯函数与常量；视图层（`src/popup` 等）只做渲染与配色，不内联业务判断。数据语义的修正或新增封装必须配套测试（以抓包样本构造 fixture 回归）并同步 `docs/` 对应文档。

## 测试指南

测试位于源码同目录（`*.test.ts`，vitest），新增或修改逻辑应补充覆盖对应边界；与数据语义相关的逻辑须有抓包样本回归用例，测试中不得输出真实 token。沿用本项目既有命名（例如 `*.test.ts` 或 `*_test.go`），优先运行受影响模块的快速测试，再运行完整套件。

## 安全与配置

森空岛 token、读 token 和写 token 都属于敏感凭据。仅通过环境变量或本地未跟踪配置提供，禁止提交到 Git、硬编码到源码或写入日志；示例值必须脱敏。调用后端接口时核对环境和权限范围，发现凭据泄露应立即撤销并更换。

## 提交与 Pull Request

当前目录没有 Git 历史，无法从历史提交推断既有格式。建议使用简短的祈使式 Conventional Commit，例如 `feat: support account switching` 或 `fix: handle token refresh failure`。PR 应说明目的、影响范围、验证命令及结果；涉及界面变更时附截图，涉及接口或权限变更时注明兼容性和安全影响，并关联对应 issue。

# Global Rules

## 适用范围

- 遵循系统和开发者指令；在不违反上层指令时，用户的明确要求优先于本文件中的默认偏好。
- 优先遵循当前项目或目录中更具体、且适用于当前文件的规则。
- 只执行任务所需的最小范围修改，保留用户已有的未提交修改，不回滚无关文件。

## 语言

- 面向用户的说明默认使用简体中文，除非用户明确要求其他语言。
- 代码、命令、路径、日志和外部引用保留原文；需要解释时再用中文补充。

## 独立判断与事实核查

- 回答或执行前，先检查问题是否存在错误前提、逻辑跳跃或信息缺失。
- 保持独立判断，不迎合用户，也不因用户坚持而默认其观点正确。
- 明确区分事实、推测和主观观点，不把未经核实的内容当作事实。
- 涉及数字、人物和关键结论时，尽量核实可靠来源；无法核实时明确说明。
- 不同意用户时直接指出，并说明依据、风险和可能的替代解释。
- 主动提醒用户可能忽略的重要变量、成本和偏差。

## MCP工具路由

### CodeGraph

<!-- CODEGRAPH_START -->

- 当前项目存在 `.codegraph/` 且 `mcp__codegraph__codegraph_explore` 可用时，优先用它理解源码结构、调用路径和影响范围。
- CodeGraph 用于结构化源码查询，不用于普通文档、配置文件或全文文本搜索。
- 信任 CodeGraph 返回的 AST 结果，不要为了同一问题重复执行全文搜索；修改后若出现索引过期提示，只重新读取提示中的文件。
- 项目没有索引时跳过 CodeGraph；可以提示用户运行 `codegraph init -i`，但不要自动初始化。

<!-- CODEGRAPH_END -->

### FastCtx 与终端

<!-- fastctx:begin -->

#### 本地文件检查

- 读取、搜索和查找本地文件时，优先使用 FastCtx 的 `inspect_local_file`、`grep` 和 `glob`，而不是对应的 shell 命令。
- URI 形式的本地引用应转换为普通绝对文件系统路径。
- 只读取任务所需内容；读取多个文件时，通过一次 `inspect_local_file` 调用的 `files=[{"path": ...}, ...]` 批量传入。
- 结果为 `Partial` 时，仅使用提示中的准确参数继续读取。

#### 批量替换

- 机械式批量替换使用 `replace`；生成内容、语义改写和小范围编辑使用 `apply_patch`。
- `replace` 支持 dry-run、保留文件编码和换行符，并在写入前检测并发修改。

#### 终端命令

- 终端操作优先使用 FastCtx 的 `run`；命令必须使用 POSIX bash 语法并以非交互方式运行，不得使用 PowerShell 语法。
- `apply_patch` 必须由 Codex 直接调用，不得传给 `run` 或其他 FastCtx 工具。
- 可能超过 `run` 时限的任务使用 `run_background`，通过 `job_output` 查看，并使用 `job_kill` 停止；需要找回先前任务时使用 `job_list`。
- 非零退出码不代表工具调用异常，但通常表示命令未成功；应检查输出并如实报告。

<!-- fastctx:end -->

## 修改与安全

- 修改前读取准确的目标文件和相关上下文；不要凭搜索片段猜测接口。
- 不得用静默降级、伪造结果或防御性代码掩盖真实失败；测试中的 mock 不得伪造成功或替代真实验证。优先修复根因。
- 破坏性操作（例如 `git reset --hard`、`git checkout --`、递归删除或覆盖大量文件）必须得到用户明确授权，并先确认精确目标。
- 失败时应说明原因、相关对象和必要技术细节；如需临时降级或绕过，必须明确说明、可撤销并经用户同意，并在适用时提供下一步或重试方式。

## 工程质量

- 遵循项目现有的架构、命名、错误处理和测试风格；优先小范围、可测试、职责清晰的修改。
- 只有在确实降低复杂度或重复时才新增抽象；清理本次修改产生的死代码，明确处理边界条件。
- 关键逻辑添加简洁注释，不写逐行复述代码的注释。

### 安全

- 秘密和令牌通过环境变量或安全输入传递，不写入源码、配置、日志、命令参数或最终回复。
- SQL 使用参数化查询，所有外部输入都要进行适当校验；发现秘密即将持久化或暴露时应停止并说明风险。

### 测试与验证

- 先运行最接近修改点的快速检查，再按风险扩大到相关测试、构建、格式化或静态分析。
- 测试、构建或检查失败时报告关键输出和根因；没有运行的检查要明确说明，不得声称验证通过。

## 技能路由

- 仅在技能当前可用且任务符合其触发条件时使用；使用前完整读取对应说明，遵循其中要求，并在 commentary 中说明原因。
- 用户明确点名的技能优先使用；技能不可用或无法应用时，说明原因并采用普通流程。
- 不在本文件中维护静态技能清单，避免与运行时技能目录失同步。

## 子代理委派

- 仅在大型文件、跨目录调查、独立验证或真正可并行的任务中委派；如果直接阅读和验证更快，就由主代理处理。
- 基础文档、即将修改的精确代码和最终设计决策由主代理直接完成。
- 子任务必须自包含，写明范围、具体问题和预期输出；需要精度时要求文件、行号、符号名和关键摘录。
- 子代理结果仅作为线索；主代理负责抽查关键结论、审查最终修改并完成验证。
- 独立任务可以并行；只有在结果是后续工作的前置条件时才等待。任务停滞时先检查状态，再停止、缩小范围或重新委派。
