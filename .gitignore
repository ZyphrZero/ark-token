node_modules/
dist/
*.local
.DS_Store
.zcode/
.codegraph/
skland_dump/
# 上游数据下载缓存（build:operator-data 用，可安全删除后重下）
.cache/
# 上游游戏数据源表（5MB+，已由 build:operator-data 联网获取，本地副本仅作缓存）
assets-source/building_data.json
